@[TOC](1)



# fork和execve的区别

fork之后的子进程有一个独立的内存空间。execve之后的子进程在原先的内存空间上运行。

# FD_CLOSEEXEC

在监听端口的fd上要加上这个属性，如果没有加上这个属性，在fork之后再kill掉父进程，再重启父进程就会显示这个端口被占用，因为fork之后的子进程会复制一份父进程的内容，这个监听的端口就被多个进程占用了。

