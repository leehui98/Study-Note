git init

git remote add origin https://github.com/lehui/web

git add -A

git commit -m "init"

git push -u origin master

git pull origin master

git checkout -b v0.0.0

git push -u origin v0.0.0