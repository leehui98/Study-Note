谈谈你对redis的理解：KV+cache+persistence 

redis的五种数据结构：字符串（字符串）、字典（哈希表）、列表（链表）、集合（哈希表）、有序集合（跳跃表）

redis的持久化：RDB(全部快照写)和AOF(单独命令追加)

CAP理论，C一致性：更新操作完成后返回给客户端，所有的结点都数据完全一致，A可用性：服务一直可用，P分区容错性：某个结点错误时，仍然可以对外表现出一致性和可用性。

redis缓存击穿

x